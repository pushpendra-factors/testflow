apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: queue-redis-pvc
spec:
  accessModes:
    - "ReadWriteOnce"
  storageClassName: ""
  resources:
    requests:
      storage: "100Gi"
  volumeName: queue-redis-pv