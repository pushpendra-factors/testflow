apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dup-queue-redis-pvc
spec:
  accessModes:
    - "ReadWriteOnce"
  storageClassName: ""
  resources:
    requests:
      storage: "100Gi"
  volumeName: dup-queue-redis-pv