apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: archive-events-job
spec:
  schedule: "30 0 * * *" # In UTC
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 5
  failedJobsHistoryLimit: 5
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            layer: jobs
        spec:
          nodeSelector:
            cloud.google.com/gke-nodepool: default-pool
          containers:
          - name: archive-events-job
            image: us.gcr.io/factors-production/archive-events-job:v0.65_276f2b4c
            imagePullPolicy: IfNotPresent
            args:
            - --env
            - $(ENV)
            - --bucket_name
            - $(ARCHIVE_BUCKET_NAME)
            - --tmp_dir
            - /tmp
            - --db_host
            - $(DB_HOST)
            - --db_port
            - $(DB_PORT)
            - --db_name
            - $(POSTGRES_DB)
            - --db_user
            - $(POSTGRES_USER)
            - --db_pass
            - $(POSTGRES_PASSWORD)
            - --max_lookback_days
            - $(MAX_LOOKBACK_DAYS)
            - --all
            - $(ARCHIVE_ALL)
            envFrom:
            - configMapRef:
                name: config-env
            - configMapRef:
                name: config-bucket
            - configMapRef:
                name: config-postgres
            - configMapRef:
                name: config-archiveeventsjob
            - secretRef:
                name: secret-postgres
          restartPolicy: OnFailure
