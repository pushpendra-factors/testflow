apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: build-seq-job
spec:
  schedule: "0 15 * * *" # In UTC
  successfulJobsHistoryLimit: 5
  failedJobsHistoryLimit: 5
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            layer: jobs
        spec:
          nodeSelector:
            cloud.google.com/gke-nodepool: default-pool
          containers:
          - name: build-seq-job
            image: us.gcr.io/factors-production/build-seq-job:v1.52_9dc1a24
            imagePullPolicy: IfNotPresent
            args:
            - --env
            - $(ENV)
            - --etcd
            - $(ETCD)
            - --bucket_name
            - $(BUCKET_NAME)
            - --local_disk_tmp_dir
            - /tmp
            - --db_host
            - $(DB_HOST)
            - --db_port
            - $(DB_PORT)
            - --db_name
            - $(POSTGRES_DB)
            - --db_user
            - $(POSTGRES_USER)
            - --db_pass
            - $(POSTGRES_PASSWORD)
            - --num_routines
            - $(NUM_ROUTINES)
            - --model_type
            - $(MODEL_TYPE)
            - --look_back_days
            - $(LOOK_BACK_DAYS)
            - --project_ids_to_skip
            - "11"
            envFrom:
            - configMapRef:
                name: config-etcd
            - configMapRef:
                name: config-env
            - configMapRef:
                name: config-bucket
            - configMapRef:
                name: config-postgres
            - configMapRef:
                name: config-buildseqjob
            - secretRef:
                name: secret-postgres
          restartPolicy: OnFailure
