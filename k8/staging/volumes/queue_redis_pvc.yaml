apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: queue-redis-pvc
spec:
  accessModes:
    - "ReadWriteOnce"
  storageClassName: ""
  resources:
    requests:
      storage: "25Gi"
  volumeName: queue-redis-pv