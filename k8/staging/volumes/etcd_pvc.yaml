apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "etcd-pvc"
spec:
  accessModes:
    - "ReadWriteOnce"
  storageClassName: ""
  resources:
    requests:
      storage: "25Gi"
  volumeName: "etcd-pv"